version: "3"

services:
  go-test:
    build:
      context: .
      dockerfile: Dockerfile
    environment:
      - PORT
    container_name: go-test
    stdin_open: true
    tty: true
    volumes:
      - .:/go/src/app
    ports:
      - ${PORT}:${PORT}